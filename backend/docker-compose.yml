version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: lexil
      POSTGRES_PASSWORD: secret123
      POSTGRES_DB: lexilearn
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
